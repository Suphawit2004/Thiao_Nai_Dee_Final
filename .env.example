NEXT_PUBLIC_SUPABASE_URL=https://YOUR_PROJECT_REF.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=YOUR_PUBLISHABLE_KEY
NEXT_PUBLIC_SITE_URL=https://your-domain.example

# Auth (Magic Link) — หลังใส่ค่าข้างบน อย่าลืมที่ Supabase Dashboard:
# 1. SQL Editor → run supabase/schema.sql
# 2. Authentication → Providers → Email → เปิด Magic Link / Email OTP
# 3. Authentication → URL Configuration → เพิ่ม Redirect URLs:
#    http://localhost:3000/auth/callback และ https://your-domain.example/auth/callback